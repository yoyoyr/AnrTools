CMAKE_MINIMUM_REQUIRED(VERSION 3.4.1)

PROJECT(LibProcinfo CXX)

SET(TARGET procinfo)

SET(
  ${TARGET}_SOURCE_FILES
  ${CMAKE_CURRENT_SOURCE_DIR}/process.cpp
)

ADD_LIBRARY(
  ${TARGET}
  STATIC
  ${${TARGET}_SOURCE_FILES}
)

TARGET_INCLUDE_DIRECTORIES(
  ${TARGET}
  PUBLIC include
  PUBLIC ../android-base/include
)